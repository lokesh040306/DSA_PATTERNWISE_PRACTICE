/*

int length(Node *head)
{
	// Linked List Traversal

    Node* temp = head;
    int nodeCnt = 0;

    // Counting nodes
    while(temp != nullptr) {
        nodeCnt++;
        temp = temp -> next;
    }

    return nodeCnt;
}

*/