/*

class Solution {
public:
    int fib(int n) {
        
        // Base case
        if(n <= 1) {
            return n;
        }

        // Processing
        // .......... int ans = fib(n - 1) + fib(n - 2);

        // Recursive Relation
        return fib(n - 1) + fib(n - 2); // ........ return ans;
    }
};

*/